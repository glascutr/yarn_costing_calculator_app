# `windows/` — Developer / Agent Guide

Purpose: Windows runner and packaging.

How to build locally:
- `flutter build windows`

Agent notes:
- Note any native dependencies and update `CLAUDE.md` for cross-platform implications.
