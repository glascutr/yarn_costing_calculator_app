# `test/` — Developer / Agent Guide

Purpose: Unit and widget tests for the app.

Guidance:
- Add unit tests for pure logic and widget tests for UI behaviors.
- Keep tests focused and deterministic.

Commands:
- `flutter test`

Agent notes:
- When adding tests, ensure they run in CI and are fast (< 5s each ideally).
