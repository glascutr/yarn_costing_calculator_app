# `web/` — Developer / Agent Guide

Purpose: Web assets and configuration for Flutter web builds.

When to edit:
- Modify `index.html`, PWAs, or web-specific assets.

How to build locally:
- `flutter build web`

Agent notes:
- Include notes about browser compatibility and any platform-specific fallbacks.
