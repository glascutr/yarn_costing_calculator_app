# `linux/` — Developer / Agent Guide

Purpose: Desktop (Linux) runner and packaging.

How to build locally:
- `flutter build linux`

Agent notes:
- Describe native resource or packaging changes here and update `CLAUDE.md` if behavior changes across platforms.
