# `ios/` — Developer / Agent Guide

Purpose: iOS-specific project, packaging, and native plugin code.

When to edit:
- Add Swift/Obj-C plugins, modify entitlements or signing.

How to build locally:
- From repo root: `flutter build ios` or open `ios/` in Xcode.

Agent notes:
- Describe native API changes and reference parent class methods in docs when applicable.
