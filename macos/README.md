# `macos/` — Developer / Agent Guide

Purpose: macOS runner and packaging.

How to build locally:
- `flutter build macos`

Agent notes:
- Update this README when adding native entitlements, menu integrations, or macOS-specific UI work.
